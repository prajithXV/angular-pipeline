import { AgentStatePipe } from './agent-state.pipe';

describe('AgentStatePipe', () => {
  it('create an instance', () => {
    const pipe = new AgentStatePipe();
    expect(pipe).toBeTruthy();
  });
});
