import { LovTypeToStringPipe } from './lov-type-to-string.pipe';

describe('LovTypeToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new LovTypeToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
