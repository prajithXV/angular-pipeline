import { AttributeTypeToStringPipe } from './attribute-type-to-string.pipe';

describe('AttributeTypeToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new AttributeTypeToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
