import { BooleanToStringOrderPipe } from './boolean-to-string-order.pipe';

describe('BooleanToStringOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanToStringOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
